# Scripted Shoot_At first pass

The PC binary with SHA-256 `b8fb9ab4c9bfc6f2868c30839d6cfc69f84b8c25d7e54eee1325f5b633c9b836` implements event type 8 in `0x4b95e0`. Its ON action iterates linked entities, clears their prior AI target through `0x409050`, sets AI action 9 via `0x407e20`, and stores the event's position from `event+0x30` at `entity+0x578`. The OFF action at `0x4ba2c0` clears the AI order and resets that point to `-1`. These facts come from the binary, not a screenshot. Raw decompiler exports are in the ignored `artifacts/analysis/rf_b8fb9ab4c9bf/` directory.

The reconstruction assigns that fixed point to linked NPCs. Existing combat cadence, finite ammunition, weapon presentation and aim turning handle repeated fire. The order does not pursue the point. Each ordinary hitscan shot or shotgun pellet selects the nearest living incidental player or NPC body along its spread ray, then uses the ordinary shield, prop, vehicle, fragment, world-cover and damage path. A dead player is excluded from body and shield interception. Attack orders can replace it; OFF clears it only while it still owns the NPC. A delayed event follows the existing event timer.

`python -B tools/check_script_shoot_at.py` runs image-free PC replays of the three authored events. L13S3 assigns its linked actor, which remains inactive in a direct neutral boot; L14S2 and L15S1 produce 35 and 20 shots respectively without player damage at the authored spawn. Staging the player by L15S1 shooter 8278 puts the player on its firing line, but ordinary fire kills the player before the delayed scripted order begins. The earlier `artifacts/xemu/render-20260926-162123/report.json` recorded 13 subsequent scripted damage hits on that dead body; those hits were incorrectly described as living-player interception. The current path excludes the dead body. A process-local handoff fixture imports 10,000 health so the staged player stays alive; the same 20 scripted shots then yield 11 incidental **player** damage shots on PC. This proves the live ray/damage path, not retail health balance. The corrected stock-64-MiB Xbox replay `python -B tools/xemu_render_check.py --no-images --level L15S1.rfl --archive levels3.vpp --actor 8278 --goto-uid 9489 --goto-frame 30 --frames 240 --seconds 600` passes 102 scene checks with zero post-death scripted player hits and exact PC/Xbox combat state, ending with 5,400 free pages (21.09 MiB). Ignored report: `artifacts/xemu/render-20260926-164503/report.json`. No images were captured or produced.

L15S1 also has an authored Invert event, UID 9491, linked to Shoot_At UID 9489. The process-local replay now admits Shoot_At as a frame-zero setup event and Invert as a timed event. Activating Shoot_At at frame 0 schedules its 1.5-second ON; activating Invert at frame 180 schedules its delayed OFF. Across 360 frames, the PC control fires 45 shots while the Invert run fires 30 and records one OFF; the current PC check still passes after the dead-body correction. The earlier `python -B tools/xemu_render_check.py --no-images --level L15S1.rfl --archive levels3.vpp --actor 8278 --setup-uid 9489 --goto-uid 9491 --goto-frame 180 --frames 360 --seconds 600` passed 102 scene checks with exact PC/Xbox ON/OFF and shot counts, ending with 5,368 free pages (20.97 MiB). Ignored report: `artifacts/xemu/render-20260926-163036/report.json`; its damage words predate the dead-body correction. This uses the actual authored link and event delay, with forced activation only to make the long chain reproducible.

The shared shotgun path now selects a fresh incidental actor for every pellet. Focused process-local PC checks cover a living player on a point ray, ordinary pellet damage, shield interception, world cover and death cutoff; the stock Xbox build includes this code. The three installed Shoot_At events link rocket or heavy-machine-gun actors, so a live authored shotgun-point encounter and native per-pellet damage remain unverified.

Open: integrate the exact original AI action-9 animation/task behavior, broader level context, an authored shotgun-point encounter and active-order save/restore. Melee point orders remain non-damaging. The ordinary save preflight still rejects active combat, firing animation and reload timing, while the NPC checkpoint has no combat-order fields and the shared spread RNG is not persisted; it cannot silently claim a restorable active order.
