# Scripted Shoot_At first pass

The PC binary with SHA-256 `b8fb9ab4c9bfc6f2868c30839d6cfc69f84b8c25d7e54eee1325f5b633c9b836` implements event type 8 in `0x4b95e0`. Its ON action iterates linked entities, clears their prior AI target through `0x409050`, sets AI action 9 via `0x407e20`, and stores the event's position from `event+0x30` at `entity+0x578`. The OFF action at `0x4ba2c0` clears the AI order and resets that point to `-1`. These facts come from the binary, not a screenshot. Raw decompiler exports are in the ignored `artifacts/analysis/rf_b8fb9ab4c9bf/` directory.

The reconstruction assigns that fixed point to linked NPCs. Existing combat cadence, finite ammunition, weapon presentation, aim turning and world/clutter impact handle repeated fire. The fixed-point order does not select the player as a direct-hit target and does not pursue the point. Attack orders can replace it; OFF clears it only while it still owns the NPC. A delayed event follows the existing event timer.

`python -B tools/check_script_shoot_at.py` runs image-free neutral PC replays of the three authored events. L13S3 assigns its linked actor, which remains inactive in a direct neutral boot; L14S2 and L15S1 produce 35 and 20 shots respectively without directly damaging the player. The stock-64-MiB Xbox check `python -B tools/xemu_render_check.py --no-images --spawn --level L14S2.rfl --archive levels3.vpp --goto-uid 10582 --goto-frame 30 --frames 240 --seconds 600` passes with identical PC/Xbox `SCRIPT_SHOOT_AT` and `ENEMY_COMBAT` words: one assigned actor, 35 shots, zero direct player hits. It finishes with 4,649 free pages (18.16 MiB). Ignored report: `artifacts/xemu/render-20260926-160840/report.json`. No images were captured or produced.

Open: integrate the exact original AI action-9 animation/task behavior, incidental actor collision on a world-point shot, OFF in a live authored chain, broader level context and active-order save/restore. The ordinary save preflight still treats type 8 as requiring unimplemented external state, so it cannot silently claim persistence of an active order.
