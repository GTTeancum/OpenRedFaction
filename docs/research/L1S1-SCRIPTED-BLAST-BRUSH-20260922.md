# L1S1 scripted-blast source correction

Read-only inspection of installed `levels1.vpp/L1S1.rfl` editor section `0x2000000` (649,126 bytes, SHA-256 `4d081acefa5d8969fcb6732ac0b5ecb60a87dd2a815bce8e618519b0abb42a26`) identified the source behind the floor near scripted `Explode` UID 9466. The event position is `(71.9004, 7.6876, 30.8536)`. Nearby compiled room-33 face 4409 is owned by editor brush UID **8756** at section offset **473977**. Its 24 source faces compile into 62 room-33 faces.

**Correction to the first report:** brush 8756 has tail flags **2**, the same subtractive-air operation as the known ctf06 air brush 66. It describes the tunnel void, not rock to remove. Its inward-oriented mesh is closed (42 exact edges, no boundary/nonmanifold/same-direction pairs) but not wholly convex: six inward halfspaces fail, with a maximum outside distance of 0.576 world units. Calling it a solid tunnel shell or a ready-made cuttable rock source was wrong. Its exact source operation and face ownership matter when a campaign room cutter is eventually built.

More importantly, [executed original-code evidence](GEOMOD-EXPLODE9466-MINIMUM-20260916.md) already proves that UID 9466 passes radius **0.75** directly to the master GeoMod gate, which rejects every radius below 1.0 before region hardness or cutter queuing. This event is a negative terrain fixture: its named blast visual continues, but its direct geometry request makes no hole. It must not drive a forced floor cut. The earlier paragraph suggesting that this specific event required a multi-solid cut was incorrect.

L1S1 `Explode` UID **9456** is a better positive campaign target: geometry flag 1, radius 5.0, position `(36.7616, 4.0061, 44.4250)`, near authored shallow/hardness-50 region 9884. Its nearby compiled room-28 geometry includes source words 3328 and 3326. Both belong to large subtractive-air brush UID 7778 (488 faces, tail flags 2) in the uniform-parser prefix; that room cannot be fed to the current single-convex DEV cutter by changing a level name. Whether UID 9456 passes all original admission gates and how its affected surface is owned still need proof before claiming a cut.

`tools/probe_campaign_geomod_brush.py` locates one UID by a validated header and parses only that brush. This avoids an extra payload after L1S1 brush index 62 that desynchronizes a uniform whole-section parser. Run `python tools/probe_campaign_geomod_brush.py L1S1.rfl 8756`; `--report <path>` emits detailed points and source counts. The probe is read-only and establishes neither runtime CSG nor a playable cut.
