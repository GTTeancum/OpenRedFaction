# L1S1 scripted-blast brush ownership

Read-only inspection of installed `levels1.vpp/L1S1.rfl` editor section `0x2000000` (649,126 bytes, SHA-256 `4d081acefa5d8969fcb6732ac0b5ecb60a87dd2a815bce8e618519b0abb42a26`) identified the real source behind the floor near scripted `Explode` UID 9466. The event position is `(71.9004, 7.6876, 30.8536)`. The nearby compiled face is room 33, source word 4409. Brush UID **8756**, beginning at editor-section offset **473977**, owns source words 4409 through 4432.

This brush has 20 source vertices and 24 authored faces. The compiled scene contains 62 corresponding visible faces, all in room 33; source face 4409 alone has 12 compiled fragments. Its exact source edges are closed and consistently oriented (42 edges, zero boundary/nonmanifold/same-direction pairs), but all 24 faces violate a single outward convex halfspace test; the maximum outside distance is 33.2144 world units. The shape is a hollow tunnel shell with floor, roof and side material. Neither outward solid nor inward cavity interpretation fits the existing single-convex-source cutter.

The `ctf06` authored-post decoder cannot be applied by changing a level name: it hardcodes selected UID/room/neighbor profiles and assumes a convex source. The L1S1 editor stream also has an extra payload after brush index 62, so a whole-section parser using uniform 20-byte brush tails desynchronizes there. `tools/probe_campaign_geomod_brush.py` locates an individual UID by validated header and parses only that brush, avoiding the unrelated extension. Reproduce the bounded ownership/topology result with `python tools/probe_campaign_geomod_brush.py L1S1.rfl 8756`; `--report <path>` writes detailed points and source counts outside the repository if desired.

The next implementation boundary is a multi-solid representation of this shell with exact authored exterior/interior faces, materials and source IDs. The floor cut at UID 9466 must affect the floor solid while preserving the two side walls and roof; shared internal partition faces must never appear in rendering or collision. Publication must replace only the affected compiled fragments in room 33 and keep stock-64-MiB memory bounded. Treating all 24 faces as a convex hull, carving a shallow visual decal, or removing compiled polygons without a real cavity would misrepresent the game's defining destruction behavior.

This is mathematical/editor-source evidence only. No campaign terrain cut or native runtime behavior is claimed.
