# Force_Monitor_Update save boundary

L6S3 cutscene UID 3696 queues event UID 6644 (`Force_Monitor_Update`, type 60) at frame 60. The queued event no longer vetoes the first-pass ordinary save: the original dispatch only invalidates a rendered monitor image, and that cache is presentation state. The checkpoint keeps the event and its deadline. The reconstructed monitor image is still absent, so this establishes gameplay continuation, not monitor visual parity.

Original `RF.exe` SHA-256 `b8fb9ab4c9bfc6f2868c30839d6cfc69f84b8c25d7e54eee1325f5b633c9b836`: allocator `0x4b69d0` routes type 60 through the base event class, whose on-action vtable entry is `0x4b9070`. Its type-60 case calls `0x4b9af0`. The raw Ghidra export of `0x4b9af0` iterates the global object list, selects object kind 4 with class flags bit `0x8` at class offset `0x74`, then calls `0x412990` with the entity handle. `0x412990` looks up a separate monitor entry by that handle (`0x412650`) and clears bit `0x4` at monitor-entry offset `0x38`. The observed operation is a forced monitor update request, not an authored-link dispatch. Type 49 (`Monitor_State`) has a separate on-action `0x4ba980` that configures the monitor entry and linked entity relationships through `0x412600` and `0x4126a0`.

The normal monitor tick `0x412200` consults bit `0x4`; render-to-texture path `0x412820` sets it when the monitor image is drawn. Thus bit `0x4` is a rendered-image validity flag, not gameplay state. A queued type 49 still vetoes save because it changes monitor configuration and linked entity relationships.

The L6S3 frame-60 image-free PC check now saves and reloads the active cutscene, retaining queued type 60. Reload preloads 1,085,298 bytes of missing NPC motion under the same 1,280 KiB ceiling as live allocation. The loader permits clustered NPC collision overlaps only where the fresh authored bodies already overlap and the restored overlap is no more than 0.01 unit deeper. It does not relax player/NPC or new NPC/NPC collision checks. PC 120-frame resumed player state matches uninterrupted 180-frame play. Stock 64 MiB XEMU now passes matching PC/Xbox save sections and 120-frame reload state, with 5,136/4,908 free pages (20.06/19.17 MiB); no images were created. Monitor imagery and broader script continuation remain open. Ghidra exports are local under `artifacts/analysis/rf_b8fb9ab4c9bf/`.

A longer PC continuation from the same L6S3 frame-60 save completes the 14-point cutscene after 3,000 resumed frames. Its event-state hash and 77-word player body match a 3,060-frame uninterrupted run; the saved first point action plus 13 resumed actions account for all 14. This tests gameplay continuation despite queued visual-only type 60. It does not test a rendered monitor image.
