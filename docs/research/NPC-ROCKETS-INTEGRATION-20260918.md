# NPC rockets

The DEV-room enemy scheduler now launches an eight-slot pool of real rockets with authored speed, lifetime, collision radius and AI-scaled damage. Successful launch owns one magazine debit; the ordinary readiness/reload/fallback path remains active and the generic hitscan path is bypassed. Flights retain their source after shooter death and update independently of the equipped player weapon. The shared rocket draw pass includes NPC flights without another model allocation. Active flights block scoped checkpoints.

A shared NPC projectile sweep covers world geometry, movers, detached geometry, other living NPCs and the player while excluding the shooter. NPC grenades use its solid-only wrapper. Rockets use liquid crossing and direct impact followed by radial damage and terrain destruction, retaining shooter attribution.

PC and Xbox builds pass. Focused collision/flight checks pass after supplying the required dry-liquid descriptor in the empty test world. Actual enemy scheduler checks prove one launch, exactly one magazine debit, retained source and no instant hitscan damage on launch or the following frame. DEV_NPC4 equips the guard with a loaded rocket and three reserve rounds. The650-frame PC encounter now records3launches/3impacts/no expired or remaining flights, and three blast hits kill the player. Final framebuffer inspected: guard holds the launcher and the death overlay is visible. A final frame does not independently prove mid-flight rendering; that presentation review remains open. Native render-20260918-104210 passes650frames with matching projectile/blast diagnostics; final native framebuffer inspected with the launcher guard and player death overlay. A subsequent correction offsets radial origin by normal*0.01, matching existing player rockets to avoid surface self-occlusion; direct contacts and terrain impacts keep the original contact. The corrected PC replay still records3launches/3impacts and lethal damage, and focused tests plus PC/Xbox builds pass. The native run predates this small offset; no native offset rerun is claimed. Campaign resource loading, predictive aim and authored attack timing remain outside this first pass.
