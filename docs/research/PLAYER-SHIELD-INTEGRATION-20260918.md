# Player shield first pass

DEV NPC mode6 adds a twelfth player slot and grants/selects riot shield solely in that opt-in fixture. Default DEV remains eleven slots and its existing supply; campaign remains four slots. Shield ownership has no ammo bank: ammo_type-1/capacity0, authored magazine32 does not create rounds. HUD publication, developer supply/refill and firing admission bypass ordinary ammo handling. The first-person view uses authored camera(-.066,.242,-.154) and65degree FOV.

The230-frame PC replay completes and its framebuffer was inspected: translucent shield and player arms are visible. The asset fixture confirms compiled fp_riotshield.v3c with idle/attack/altfire.rfa, no reload,20bones,775016resident and786612peak bytes under1MiB. Adding the view references21380676image bytes, exceeding the normal20MiB image admission. Mode6 alone permits21MiB on both renderers; stock64MiB runtime admission is still required. Default profiles retain20MiB and do not load the new view. PC/Xbox builds pass; Native render-20260918-111807 passes230frames; framebuffer inspected and1808pages (7.06MiB) remain on stock64MiB. This initial native run establishes view/memory, not blocking.

Focused pose/lifecycle helpers pass and are now wired to direct NPC gunfire. The view contains arms and has independent FOV/depth: do not use its projected silhouette as a collision hull. The pose helper resolves fk-shield from evaluated animation and transforms through the authored camera offset into world space. Physical collision uses only authored fk-shield/descendant-weighted triangles (256installed triangles), excluding hand/arm and mixed-influence vertices. It applies the renderer skinning transforms then unprojected camera offset and world eye basis; this is explicit port collision policy, not use of overlay FOV/depth. Shared damage arithmetic supplies authored1250durability and a lifecycle helper preserves damage through weapon switches, requests removal/fallback once on break, and resets only on a new acquisition or newplayer.

Authored bash10damage/range2/wait.5/impact-delay.2, shield pickup and saves remain. RFCP explicitly rejects owned player shields until durability is represented; existing ten-base-weapon catalog check passes. No alternate gameplay is inferred solely from the alternate animation.

## Incoming shots and breakage

Shield pose advances once before enemy attacks; drawing reuses that current pose. Ordinary NPC and shotgun ray paths query the physical shield before cover, clip obstruction checks to its contact and then commit source-attributed durability before player body damage. Blasts/scripts/projectiles remain outside this direct-ray adapter.

Live PC650-frame mode6 encounter blocks17enemy shots with player health100. The1800-frame continuation blocks32hits, breaks once at-30durability, selects handgun slot0, then takes5body hits and dies. Both final framebuffers inspected: intact shield in short encounter and no shield/death overlay after break. Native render-20260918-112242 passes650frames with exact17block/durability/source counters, unchanged player health and1806free pages (7.05MiB). Native framebuffer inspected with shield intact. Native break continuation has not been run. The authored bash timing helper passes separately but is not yet wired to contact damage.
