# Campaign undercover first pass

The five authored `Go_Undercover` events (type 47) now have a gameplay handler. The event's first word selects the form: zero is `parker_suit`, and nonzero is `parker_sci` in the original executable. The original dispatch and entry/exit services are at `4bcdb0`, `4bcdc0`, `4b00f0`, and `4b0380`; binary-derived contracts and the original executable hash are in `docs/research/FUTURE-CAMPAIGN-AI-MODE-UNDERCOVER-20260915.md` and its entry, exit, and identity follow-ups.

On entry the port requests the distinct `Undercover 12mm handgun` resource from the level's event demand, grants and selects that weapon, remembers the previous selection, clears current and class armor, clears player flag `0x40` at `0x810`, and sets bit `0x10000` at `0x7d0`. The player body and health remain live. Ordinary unalerted guards do not newly acquire an uncompromised disguised player; pre-existing/scripted combat continues. On event OFF the port restores the normal class armor and both flags, then tries to restore the previous selection. The form is carried in memory for level transitions, and a death restart clears it. These are first-pass gameplay approximations: the current player entity and camera are not recreated as the original does, and the full inventory constructor/transfer contract is not established.

The ordinary environment checkpoint now writes RFEN4 with a 24-byte player-form record after the cutscene record. It stores activity, exact authored variant, compromise state, previous weapon slot and unarmed state, and the normal class armor. Preparation validates these fields and older RFEN1/2/3 saves still decode as no active form. The loader admits the undercover weapon resource and ownership before publication, restores the form and flags with the player inventory, and retains it for a later in-memory section transition.

Validation on 2026-09-25/26: the PC resource-demand, core ON/OFF dispatch, event-checkpoint, and environment-checkpoint tests pass. An image-free neutral replay of L6S3 with authored UID 6938 enters variant zero and selects weapon ID 4. A 120-frame stock-64-MiB XEMU replay quick-saves at frame 60 and quick-loads at frame 90. Both platforms report successful save and load of a 65,660-byte checkpoint, and the restored `PLAYER_FORM 1 0 0 0 0 4 0 0`, all player-ammo words, and player body match. XEMU reported 4,988 available 4-KiB pages at the endpoint. The generated run report is `artifacts/xemu/render-20260926-133228/report.json`; the no-images run created no raster files. The prior 90-frame entry-only run is `artifacts/xemu/render-20260926-132045/report.json`.

L8S1 UID 6447 exposed a frame-zero ordering bug: the event was ready, but the immutable weapon IDs had not been bound and combat initialization could erase its grant. Base inventory and weapon identities now initialize before startup events; frame-zero combat retains that inventory. A separate 90-frame image-free stock-64-MiB XEMU run reports `PLAYER_FORM 1 1 0 1 0 4 0 0` and `PLAYER_AMMO 4 125 16 0 0 0 448 0`, both exactly equal to PC, with the player body equal and 5,874 available pages. The report is `artifacts/xemu/render-20260926-134324/report.json`; it created no raster files. PC L6S3 suit entry and L1S1 neutral control also completed after the startup change.

Authored handoffs now have a repeatable image-free PC check in `tools/replay_undercover_handoff.py`. L6S3 setup UID 6938 enters the suit, and the linked Invert UID 6939 removes it after its authored delay; a 240-frame PC/Xbox run ends with normal form, armor 100, and the prior weapon selected. Xbox exactly matches `PLAYER_FORM 0 0 0 1 1 4 1120403456 0` and `PLAYER_AMMO 3 125 16 0 0 0 448 0`, with 5,187 available pages (`artifacts/xemu/render-20260926-135258/report.json`). A 120-frame run is too short to assert the OFF result. L8S1 setup UID 6447 enters the scientist form; process-local exits 5625 and 5623 drive L8S1→L8S2→L8S1. The 240-frame PC and stock-64-MiB Xbox runs end with the form and handgun intact. Xbox reports the same full `PLAYER_FORM 1 1 0 0 0 4 0 0` and `PLAYER_AMMO 4 125 16 0 0 0 448 0` words as PC, two matching transitions, and 5,865 available pages (`artifacts/xemu/render-20260926-134848/report.json`). Both Xbox runs created no raster files. These are forced process-local exit dispatches, not a walked campaign route.

On an in-memory section carry, the next section now loads the matching player class from `entity.tbl`, its distinct compiled mesh, physics configuration, and class motion set. The placed animation stream accepts that mesh name instead of always opening `miner.v3c`. Binary asset inspection shows all three meshes have 25 bones with the same names and parent layout; their bind transforms differ. The image-free player-model telemetry records the selected class, compiled mesh bytes, and decoded bone count. PC and stock-64-MiB Xbox L8S1→L8S2→L8S1 both end with `PLAYER_MODEL 2 80555 25 0` (`parker_sci.v3c`), with 5,843 Xbox pages available (`artifacts/xemu/render-20260926-135953/report.json`). PC and Xbox L6S2→L6S3 both end with `PLAYER_MODEL 1 77931 25 0` (`parker_suit.v3c`), with 5,172 Xbox pages available (`artifacts/xemu/render-20260926-140438/report.json`). Both runs created no raster files.

Live ON/OFF now changes the borrowed animation model, 25-bone bind pose, class motion map, collision spheres, stance cache, eye tag/offsets, eye flags and player material on the next frame. The replacement model and class pose are prepared before the body/model commit; the active playback controller restarts in the new class. State-set and physics metadata for all three player forms are loaded only when the undercover weapon resource is demanded. The image-free L8S1 scientist ON replay ends with `PLAYER_MODEL 2 80555 25 1` on PC/Xbox and 5,857 available Xbox pages (`artifacts/xemu/render-20260926-141714/report.json`). The L6S3 suit ON→linked OFF replay ends with `PLAYER_MODEL 0 88108 25 2`, matching full player body and form words on PC/Xbox with 5,170 Xbox pages (`artifacts/xemu/render-20260926-142006/report.json`). A separate L6S3 quick-save at frame 60 and quick-load at frame 90 restores the suit model and player body exactly on PC/Xbox; the 65,660-byte checkpoint passes with 4,955 available Xbox pages (`artifacts/xemu/render-20260926-142641/report.json`). PC neutral L1S1 stays on the miner model. No images were produced.

Disguise compromise now follows a bounded port rule: a shot compromises the form when its hearing event alerts an eligible hostile guard, or a direct hit leaves an armed NPC alive and alerted. The attached Undercover suppressor suppresses only that shot-hearing event; a victim can still react. A shot into an empty section no longer destroys the disguise merely because it fired. This is not an established original-game compromise predicate. A process-local L8S1 replay on PC confirms the scientist remains disguised after an empty shot, with or without an attached suppressor. The attached case also passes a 240-frame image-free stock-64-MiB XEMU replay: both platforms report one mode change, one suppressed shot, `PLAYER_FORM 1 1 0 1 0 4 0 0`, identical combat/ammo state, and 5,841 Xbox pages available (`artifacts/xemu/render-20260926-143917/report.json`). The run created no raster files.

`tools/replay_undercover_compromise.py` stages the player near L8S1's alarm-linked actor 6467, dispatches only Go_Undercover 6447, then replays a single shot at frame 170. Actor 6467 is hidden in this section; two other nearby eligible guards hear the loud shot. In a 240-frame image-free stock-64-MiB Xbox replay, that shot compromises the disguise (`PLAYER_FORM 1 1 1 1 0 4 0 0`); all selected gameplay words match PC and 5,735 pages remain (`artifacts/xemu/render-20260926-144624/report.json`). The same encounter with the suppressor attached records one accepted suppressed shot, no enemy combat alert, and `PLAYER_FORM 1 1 0 1 0 4 0 0` on both platforms with 5,735 pages available (`artifacts/xemu/render-20260926-144826/report.json`). Neither run produced raster files.

The replay's direct-hit case instead stages exposed armed actor 6270. A suppressed shot at frame 170 hits that NPC, leaves it alive, alerts it and compromises the scientist disguise. PC and the 240-frame stock-64-MiB Xbox run match on one accepted shot, one actor hit, no death, `PLAYER_FORM 1 1 1 1 0 4 0 0`, and the actor's remaining health; 5,774 Xbox pages remain (`artifacts/xemu/render-20260926-150037/report.json`). The native run used `--no-images` and created no raster files. Witness line of sight and the exact original compromise rule remain unverified.

The handoff replay also walks the disguised scientist through L8S1's real exit volume linked to UID 5625. With the player staged outside that authored trigger, input begins forward movement at frame 30 and the ordinary collision/trigger path transfers to L8S2 at frame 58. A 240-frame image-free stock-64-MiB Xbox run matches PC on the transition frame, the intact scientist form, the undercover weapon/ammo and `PLAYER_MODEL 2 80555 25 0`; 6,169 pages remain (`artifacts/xemu/render-20260926-145420/report.json`). The staging starts near this exit, so it does not establish a complete walked campaign route. The run produced no raster files.

Remaining work: the original recreates the player and camera objects, while the port currently updates the existing body and eye inputs; failure after event gameplay publication does not yet roll back the form if a model allocation fails. First-person body rendering is suppressed and held-hand appearance still needs confirmation. Establish exact inventory/weapon ownership and witness/disguise rules; test naturally triggered OFF flows and other exits, and broaden active-form save/campaign coverage. These image-free replays establish asset selection, collision/eye inputs and gameplay state, not visible appearance or final visual fidelity.
