# Campaign cutscene resources and runtime, first pass

The shared PC/Xbox core now owns bounded v180 level sections `0x400` (cameras), `0x4000` (timelines), `0x5000` (control points), and `0x6000` (paths). It decodes the original 16-bit length-prefixed strings, reordered camera basis, descriptor selectors/FOV/hide flag, ordered point durations and words, and four-control cubic paths. Camera and path joins are validated before publication; a failed load leaves the previous owner unchanged. One allocation holds the retained records, while the temporary source sections are charged to the caller's peak budget and released after loading. Source references: [timeline loader research](research/FUTURE-CAMPAIGN-CUTSCENE-TIMELINES-20260915.md) and [camera/path arithmetic](research/FUTURE-CAMPAIGN-CUTSCENE-CAMERA-PATHS-20260915.md).

The installed-level check loads all 12 authored cutscene levels and all 84 timeline points, including L6S3 UID 3696's 14 points, 15 cameras and two paths. Its `pleasework` midpoint samples within 0.0005 of the original-code result. The focused PC check and the NXDK image build pass.

The first runtime pass now dispatches event type 55 into a scene-owned cutscene clock, installs the selected camera, advances authored points and cubic paths, fires each point's linked action, masks player input, and restores normal camera/input when the timeline finishes. Event type 83 receives the completion pulse. L6S3's opening point starts both authored actor animations and its sound chain. Their RFA payloads total 1,085,298 bytes, so the bounded resident-motion ceiling is now 1,280 KiB; this allocates on demand, and the stock-memory XEMU run had 5,139 physical pages (20.07 MiB) available at its endpoint.

L6S3 UID 3696 completed a 300-frame neutral replay on PC and stock 64 MiB XEMU. The inspected native framebuffer shows the authored room and Gryphon and matches the PC frame. A later 3,060-frame PC replay traversed the full 47.82-second authored timeline: all 14 point actions fired, the type 83 completion chain restored the three disabled navpoints, teleported the player, started Gryphon's movement, and displayed `You lead the way.`. The cutscene became inactive with zero recorded errors. Sixty frames of post-completion forward input moved the player from `(1.165927,-4.118478,-23.027193)` to `(4.426909,-4.118478,-19.424313)`. This proves PC control restoration for that staged path; the long walking fixture also re-enters an authored trigger because it staged the cutscene early.

The authored 45-degree camera FOV now scales world projection after combat's weapon-scope update; gameplay HUD/reticle hide during the cutscene while subtitles remain available. The inspected PC and XEMU opening frames show the same Gryphon close-up instead of the former wide gameplay view. A stock 64 MiB XEMU replay of the complete 3,060-frame sequence passes the shared state checks, including exact 12-word `CUTSCENE`, animation and scripted movement state. Its inspected final frame matches PC's open-door Gryphon handoff and subtitle; 5,061 physical pages (19.77 MiB) remained available. The Xbox run used neutral input, so post-cutscene movement is directly confirmed on PC and native hands-on movement remains open. Camera look-at, skip/cancel behavior, other authored sequences, save/load during a cutscene, and broader timing/parity review remain.

The teleport placement adapter now uses the original `4a0770` full-body sweep: from 0.25 units above the authored destination to 2 units below, with 0.05 units of clearance on a hit. Query flags come from the body state (`state_124|4`, plus `0x100` for a tiny body), and the scene collision facade includes committed movers. A 3,000-frame L6S3 PC replay after this change completed normally and remained supported at `(1.165927,-4.121857,-23.027193)` through frame 2999. The NXDK image builds; native replay of this particular placement change is still open.

L11S3 UID 10626 now traverses its full 23-point, roughly 99-second timeline and dispatches completion UID 10655, including Teleport_Player UID 10654 and the self-destruct message. The 5,870-frame PC trace shows the player at the authored `(52.779510,-70.885880,-152.347610)` on frame 5848, then falling with neutral input. The destination is above an open shaft in retained room 2: the original 2-unit downward body sweep misses, an actual 12-unit static-world ray also misses, and nearby mover faces do not produce a normal body hit. This is an input-sensitive handoff, not evidence for extending the original sweep or adding an arbitrary floor.

The process-local setup harness now admits type 63 directly, enabling a 180-frame replay without waiting through the whole cutscene. Holding forward from UID 10654 reaches the lower static floor by frame 60 at player Y `-75.464836` (floor `-76.34375` plus body clearance), remains at that Y through frame 179, and collides with the far wall instead of falling into the void. The same focused replay passes on stock 64 MiB XEMU: all 77 compared `PC_PLAY_BODY` words match PC, and 4,988 physical pages (19.48 MiB) remain. A separate 6,030-frame PC run through the complete L11S3 timeline, completion chain and immediate forward input reaches the same supported Y by frame 5960 and remains there through frame 6029; CUTSCENE reports 23/23 points and no error. Native full-timeline input and natural campaign traversal remain open. Local telemetry: `artifacts/cutscene-l11s3/placement-5870.txt`, `direct-diag3.txt`, `forward-180.txt`, `handoff-forward-6030.txt`, `artifacts/xemu/render-20260925-205232/report.json`, and `artifacts/cutscene-l6s3/placement-check.txt`.

The three authored look-at points (L14S3 miner UID 9642 and bottle UID 10669, L17S4 effect prop UID 18250) now resolve live scene owners where available after each camera position update. NPCs use their published world position with absolute world Y replaced by the existing entity eye field, matching the executed original instruction path. The bounded orientation helper matches the original +X and oblique matrix cases within 1e-5; missing or coincident targets retain the preceding orientation. The PC resource/math test and PC/Xbox builds pass. Model attachment targets and the exact source of the original entity vertical field still require verification. Original-code research has not identified a skip command, so none is claimed here. User requested no images; this pass used code and numerical checks only.

Live PC camera targeting now passes `python tools/check_cutscene_look_at.py` with an image-free process-local replay mode. L14S3 starts on miner UID 9642 (two successful aims) and reaches its last bottle UID 10669 point (336 successful aims, no missing targets or errors). L17S4 starts its cutscene during normal level startup; effect-only clutter UID 18250 has no model/body owner, so the camera uses its authored world placement (two successful aims). The setup harness must not fire that already-active L17S4 cutscene again. The new PC `--spawn-telemetry-replay` command advances the shared scene and prints state without rasterizing or writing a frame. Attachment and moving-effect behavior remain open. No image was generated or captured for this check.

The stock 64 MiB XEMU harness now supports `--no-images`, using PC telemetry replay and skipping framebuffer capture even on an error path. Two 32-frame native runs pass with all six `CUTSCENE_LOOK` words identical to PC: L17S4 target UID 18250 has 32 aims, no misses/errors, 32 authored-position fallbacks and 8,693 free pages (33.96 MiB); L14S3 miner UID 9642 has 32 aims, no misses/errors, no fallbacks and 4,332 free pages (16.92 MiB). `CUTSCENE` state also matches exactly in both runs. Reports: `artifacts/xemu/render-20260925-211739/report.json` and `artifacts/xemu/render-20260925-211900/report.json`. Neither run created a frame file. These checks establish camera state and stock-memory fit, not visual parity; native L14S3's later bottle point and longer active-cutscene behavior remain unverified.

The ordinary same-level checkpoint now carries an `RFEN3` cutscene record. It stores the active selector, authored point, path phase, camera pose/FOV and remaining timers without serializing pointers; fresh-scene restore rebinds resources and timers to the new clock. RFEN1/2 remain readable, and loading an inactive record cancels any boot-time cinematic. The L6S3 focused test restores a point after a clock rebase and advances to the correct next action without refiring the saved point. L1S1 image-free live PC save/reload passes, as do stock 64 MiB XEMU save and load with the environment section byte-identical to PC; the native save ends with 4,260 free pages (16.64 MiB), and load with 4,179 (16.32 MiB). Reports: `artifacts/xemu/render-20260925-213746/report.json` and `artifacts/xemu/render-20260925-213956/report.json`. No image files were created.

L17S4 now admits a live active-cutscene save. Its pending type 61 `Black_Out_Player` event (UID 20698) retains its timer through the event checkpoint, and the active blackout overlay's remaining timer is carried in RFEN3; the unmodeled visual-event blocker was removed for this specific event. Effect-only clutter UID 18250 can legitimately be absent from the live body registry, and the saved player position is admitted inside the exact authored Escape Pod UID 13627 spawn shell. A one-shot startup snapshot guard prevents the L17S4 save from being reapplied after the restored timeline hands off to L18S1; explicit live quick-load still works. The image-free PC check saves at frame 60, reloads the active timeline, and reaches L18S1 at restored frame 1062. Stock 64 MiB XEMU save/load matches the PC checkpoint and 12-word cutscene state with 8,531 free pages (33.32 MiB) after load; reports: `artifacts/xemu/render-20260925-215745/report.json` and `artifacts/xemu/render-20260925-215838/report.json`. Other cutscene levels can be blocked by NPC or event admission.

The initial image-free 1,200-frame comparison mistakenly paired a frame-65 Xbox HDD save with a frame-60 PC save. Their RFEN3 cutscene deadline differs by 50 ms, explaining the apparent three-frame handoff offset and the failed fragment tick check in `artifacts/xemu/render-20260925-220943/report.json`. The corrected run loads the PC save generated with that same native frame-65 capture; all 16 checkpoint sections are byte-identical. PC and XEMU both hand off L17S4 to L18S1 at frame 1059, finish 1,200 frames with 125 fragment ticks, and pass the checked gameplay-state comparison. XEMU has 5,065 free pages (19.79 MiB) at the endpoint. Report: `artifacts/xemu/render-20260925-222047/report.json`. Neither run created an image file. This establishes restored L17S4 handoff parity for the neutral-input fixture, not general cutscene save coverage or visual parity.

L14S3 now admits a live save at frame 60 of cutscene UID 9618 while the player stands on authored mover UID 6711. RFEN3 stores that UID instead of the runtime support handle. Restore stages the environment before player placement, checks the exact candidate mover and upward support contact, and rebinds its handle and velocity only after all checkpoint sections validate. Event type 63 `Teleport_Player` is admitted by the event checkpoint. One stationary NPC slightly rotates within an unchanged authored prop's coarse sphere; restore admits only this already-overlapping authored sphere pair at its original position, with bounded rotation and no more than one-percent extra overlap. The image-free PC check compares all 77 `PC_PLAY_BODY` words after 120 resumed frames against an uninterrupted 180-frame run. Stock 64 MiB XEMU stores the same 65,200-byte, 16-section checkpoint as PC and reloads to the same active cutscene and player body state, with 3,977 free pages (15.54 MiB). Reports: `artifacts/xemu/render-20260925-224014/report.json` and `artifacts/xemu/render-20260925-224232/report.json`. This establishes the initial neutral-input support fixture; longer moving-support dynamics, other cutscenes, later bottle playback, full-timeline input and cancellation remain open. No images were captured or generated.

L11S3 now also saves at frame 60 of active cutscene UID 10626 and resumes for 120 frames with the same 77-word player body as uninterrupted 180-frame PC play. Its inert `Turn_Off_Physics` event (type 62) is represented by the generic event checkpoint; a queued unimplemented dispatch remains a save veto. The original Capek spawn intersects unchanged mover UID 4937, so candidate admission accepts that exact overlap only on fresh boot with identical authored NPC, mover pose, collision view and geometry. The player settles 0.519 units below the authored start and intersects the unchanged coarse sphere of OfficeLight_Lg01F UID 10332; admission requires the original start X/Z and body basis, a bounded downward settle, unchanged prop identity/pose and no deeper overlap than at spawn. The PC focused check and stock 64 MiB XEMU save/load pass without image capture. Native save has 4,845 free pages (18.93 MiB) and load has 4,609 (18.00 MiB); reports: `artifacts/xemu/render-20260925-230411/report.json` and `artifacts/xemu/render-20260925-230554/report.json`. The additional frame-300 L14S3 PC check saves after cutscene point 1 and matches uninterrupted player state on reload; other timeline actions still need coverage.

L6S3 now also saves at frame 60 of active cutscene UID 3696 and resumes for 120 PC frames with the same player body as an uninterrupted 180-frame run. Original binary tracing shows queued `Force_Monitor_Update` (type 60) clears a rendered monitor-image validity bit, so the event is retained as a visual requirement without blocking first-pass gameplay save. Restore preloads two active actor clips under the existing 1,280 KiB cache budget. It allows only pre-existing authored NPC/NPC collision overlap that does not deepen by more than 0.01 unit after restore; player contact remains strict. Stock 64 MiB XEMU save/load passes with all 16 checkpoint sections identical to PC and 120-frame player/cutscene state matching, leaving 5,136/4,908 free pages (20.06/19.17 MiB). No images were created. Monitor visuals and broader script continuation remain open (see `docs/research/FORCE-MONITOR-UPDATE-20260925.md`); reports: `artifacts/xemu/render-20260925-233613/report.json` and `artifacts/xemu/render-20260925-233751/report.json`.
