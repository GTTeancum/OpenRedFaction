# Collecting AI: bounded implementation preparation

This is prop hauling, not weapon/ammo collection. The destination helper is implemented independently; live collecting gameplay is not implemented by this change.

Evidence comes from the installed RF.exe SHA256 b8fb9ab4c9bfc6f2868c30839d6cfc69f84b8c25d7e54eee1325f5b633c9b836, inspected directly without running the original game. The local Dash Faction `rf/ai.h` supplies corroborating submode names and `rf/clutter.h` identifies the clutter list sentinel at005c9360.

- 004053a0 dispatches submodes4 patrol,5 pickup,6 move to drop,7 drop. Patrol invokes00405460, pickup00405540, drop004058e0; move to drop advances the existing navigation path.
- 00405460 scans the clutter list (head005c95ec, next offset28c) every400ms. Eligible object flags7c contain1000 and lack2000. The first eligible object within40 world units with a clear498e80 query is selected. Selection reserves flag2000; this is not nearest-object selection. Clutter construction4109d5 maps class flags bit0 into object1000. The exact authored class property name is not yet established.
- 00405540 approaches the target, using target position plus1.1 height, with pickup proximity0.5. Animation23 begins when supported, with a5500ms attachment timestamp. Attachment427240 uses the actor attachment array8cc slot0 and sets object flag100. Missing animation takes an immediate attachment path. Animation completion selects the drop destination; failed navigation switches to catatonic mode1.
- 004057f0 scans all events, accepts runtime type27 (`Drop_Point_Marker`), and selects strictly smaller full3D Euclidean distance. Equal distances retain the first event. Position comes from event40 and is copied into AI490. It applies no link or radius filter. No marker returns-1. Distance helper4faed0 subtracts vectors then calls40a000 (square root of sum of squares).
- 004058e0 starts drop animation22 when supported, schedules125ms release, detaches using427380, clears the target, and enables dropped-object physics flags1a8 with80000001. Animation completion returns to patrol4. There is additional post-release object handling through410c70 that remains to be reconstructed before live integration.

`scene_collecting_destination_find(events, origin, result)` uses owned level events directly and returns index, UID, copied position and distance. It allocates nothing and preserves output on failure. It uses double intermediates for portable finite3D distance and explicit malformed-input rejection; it does not promise x87 rounding parity at extreme/tied near-equal coordinates. Tests cover nearest selection including height, deterministic exact ties, unrelated event rejection, no markers, and atomic malformed-input failure.

Integration still requires eligible movable clutter registration/reservation, actor attachment slots and carried-object transform propagation, pickup/drop animation binding, navigation completion transitions, release physics, and cancellation/death cleanup. Do not admit mode5 merely because a destination exists. Parent can include the helper near AI gameplay adapters and add the focused test target linked to rf_core; no scene wiring or build files changed here.

## Attachment and post-release follow-up

The remaining410c70 call is just handle lookup plus type4 admission. The following4059c4..4059dd selects10 for an even handle or11 for an odd handle, then calls410cf0 with0.5 seconds. That routine schedules the clutter delayed-kill timestamp at2a4 and stores the delayed-kill sound at2a8. Its milliseconds scale589410 is1000. Existing `rf_clutter_state.timer_a4` and `word_a8` are those fields.

The new `scene_collecting_clutter_lifecycle.inc` supplies exact flag eligibility, synchronous reservation, and post-detach release against that existing state. `scene_collecting_clutter_reserve` adds2000 only when1000 is set and2000 is clear. `scene_collecting_clutter_released(state, now)` must run after successful actor detachment: it adds physics flags80000001, schedules500ms with `rf_timer_set` (including wrap), and stores sound10/11. Other fields and flags remain untouched; in particular it does not clear100 or2000 and cannot make a successfully delivered prop collectible again. It does not execute the delayed kill. Invalid calls leave state unchanged. The focused test requires rf_core, and was written without running a build.

427240 takes actor `this`, child handle and attachment identifier. It scans actor8cc slot pointers and chooses the last matching identifier (slot word0); the slot's child word4 must be-1. After resolving the child, it writes actor handle to child200, identifier to child204, and child handle to slot word4. It clears the child's vectors144/150, with separate sound and special-entity side effects. This routine does not calculate a pose or supply a new offset: collecting passes the identifier stored in slot0. Existing public clutter state lacks child200/204 and the actor slot table; no synthetic attachment fields were added by this helper.

427380 finds the first slot holding the requested child handle and clears the slot before looking up the child. If child lookup succeeds it clears child200 to-1, optionally plays the detach sound, clears actor byte720, and updates a special-entity auxiliary field. It does not clear child204 or the collecting reservation flags. A stale child handle still frees its slot, but returns failure. Pose propagation is a separate attachment-update binding.

Cancellation must retain a generation-bearing target handle and reservation owner, clear only that actor's uncompleted reservation, stop outstanding pickup/drop timers, and release the slot even if its child disappeared. Do not call the completed-delivery helper when merely cancelling before pickup. For an already attached prop, detach and restore standalone physics/pose before removing the actor; whether an interrupted delivery should also schedule destruction is not established here. The reconstructed418f80 finalizer already expresses DETACH_CHILD(handle,1) for every attachment; its scene backend must bind actual carried clutter too. Clearing only the AI target would leak the slot or leave an orphaned carried object. Scene unload and prop destruction require the same two-sided ownership cleanup. These are integration requirements, not claims of completed live handling.

Parent verification: `rf_scene_collecting_destination_tests` builds and passes on PC. This is a helper check only; no live hauling or Xbox hauling claim is made.

## Scene preparation and installed-data scope

Scene clutter creation now maps class flag bit0 (`collectable`) to object flag0x1000, as in4109d5. This makes real Shard2/Shard3 owners eligible for the future hauling scan without reserving or moving them. PC and NXDK Xbox builds pass after this integration. The lifecycle helper also passes its focused PC checks, including timer wrap and nonmutation on invalid input; it remains preparatory code, not a live delivery scheduler.

`tools/inspect_collecting_scenes.py` reads all94 installed levels directly. It finds23 Set_AI_Mode events (14 authored enum0 and9 enum1), no authored collecting enum3 and no Drop_Point_Marker records. L1S1 Grabber8367 is2.21 units from Shard2 UID8669, with Shard3 UID8670 nearby. The Grabber class has rock pickup/drop animations but the scan does not establish automatic mode initialization. These facts do not prove hauling is absent from the original runtime. Keep full hauling integration deferred pending its activation/attachment contract, rather than enabling mode5 from proximity alone.
