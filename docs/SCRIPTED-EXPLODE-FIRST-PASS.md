# Scripted Explode radial first pass

`Explode` type 10 now reaches the shared world blast service on ON and supports delayed activation. It resolves the event's `texts[0]` through `vclip.tbl`, loading only clips used by the current level during scene setup. An unresolved/empty clip uses base damage 1; a resolved clip uses its authored `$damage` (usually 0). The delivered magnitude is a final float cast of `base * values[0] * values[1]`, radius is `values[0]`, source is `UINT32_MAX`, kind is 3, and the origin is the exact event position. OFF is a no-op. This is the world-wrapper arithmetic observed at original `RF.exe` 0x436490, 0x4364da..0x4364eb and 0x4365fe..0x43660d; see [the executed binary research](research/secondary-re/campaign-explode-world-20260916.md).

Focused dispatcher checks pass. Process-local PC L1S1 UID 9395 delivered a named `charge_explode` request with zero radial damage; L9S3 UID 137 delivered the empty-clip positive radial request after its one-second authored delay. PC and NXDK builds pass. These replays establish event reachability and numeric dispatch; they do not establish actual victim damage in those placements, native runtime behavior, visual effects, or terrain cuts.

The original calls GeoMod before the world wrapper when flags[0] low byte is 1, then requests a named visual, then radial damage. Named clips now own their authored explosion recipes and central emitter images under a four-clip, 512 KiB scene budget. The scene loads only clips used by the level and resolves the visual room from the event position. The central emitter timing/scaling follows the existing `rf_explosion_clock_tick` and `rf_explosion_central_prepare` implementation of original 0x48e290 and 0x48e7c5..0x48e879; see [explosion recipe research](EXPLOSION-RECIPES.md). The provisional emitter direction is unit X. The vclip's separate VFX, sparks, trails, full sound behavior and original visual direction remain open.

L1S1 UID 9466 (scale 0.75, zero radial damage, geometry flag 1) created four central emitters in a five-frame process-local PC replay. A native PC raster near that event showed a blast around the miner; the same camera without activation had no blast and differed in a bounded 210×108-pixel region. The new headless inspection camera allows this campaign view without host input. The Xbox image builds, but an existing user XEMU session was left untouched, so native timing, memory headroom and rendered visuals are unverified.

Campaign GeoMod remains missing: the present cut owner is limited to the DEV terrain fixture. The scene counts geometry-flag requests but does not substitute the rocket launcher crater. Authored GeoMod regions and default hardness now load in ordinary levels, while room/terrain ownership, effects in ordinary saves and native runtime checks remain open. Zero radial damage does not suppress the named visual path or an eligible terrain request.

Correction: L1S1 UID 9466 requests radius 0.75, below the original master GeoMod minimum of 1.0, so it should **not** cut terrain even after campaign integration. Its visual still dispatches. L1S1 UID 9456 requests radius 5.0 near authored soft region 9884 and is the relevant positive-cut target. See [the original-code minimum-radius evidence](research/GEOMOD-EXPLODE9466-MINIMUM-20260916.md) and [the source-brush correction](research/L1S1-SCRIPTED-BLAST-BRUSH-20260922.md).

The shared scene now reads GeoMod region records and the level's stored default hardness for ordinary campaign loads, without allocating a terrain cutter yet. Focused five-frame process-local PC replays loaded 77 L1S1 regions in 5,852 bytes (stored default hardness 0), preserved the ctf06 DEV region path, and loaded L6S2 with zero regions when section `0x200` is absent. PC and NXDK builds pass. This proves resource ownership, not region admission or a visible campaign cut.
