# Jeep first playable integration

Select `RF_REPLAY_VEHICLE=jeep` on PC, or `--vehicle-test --vehicle-class jeep` in the XEMU harness. This is the enemy-free DEV fixture, not campaign placement. `tools/check_jeep_replay.py` generates a 460-frame seated gunner replay; `--exit` generates a 600-frame driver/gunner/driver/on-foot sequence.

Actual Jeep01 class resources provide chassis, collision, suspension, health400, interface_1 driver position and interface_2 gunner position. The driver uses jeep.vfx. Tab/D-pad-right switches roles while parked (linear and angular speed at most0.1); driver can steer but cannot fire, gunner can fire but cannot steer. One registered host and one player retain possession throughout; there is no simulated NPC driver.

The interface_2 quaternion makes its forward axis point downward. Binary-derived occupant basis-lock evidence (487630/48a230 and4194e0) requires independent gunner orientation: only position follows the attachment. The adapter retains player body basis and exact tag eye position. Relative gunner aiming uses an explicit playable port policy of +/-60degree pitch and +/-180degree yaw; this is not claimed as the original turret limit. Returning to driver restores the authored driver basis, and exiting restores saved on-foot look.

Seated transfers use the actual top player sphere against world/movers, rather than dragging standing legs through the floor. Exit still requires a full standing-body downward sweep, walkable support and clearance from the chassis. The bounded standing exit seed can conservatively reject low ceilings; general crowded vehicle ejection is not claimed.

The mounted gun uses the real jeep_gun attachment and jeep_gun.v3m, including Muzzle_1. Gun residency553656bytes and load peak563048bytes fit the576KiB cap. Gun articulation, rendering and projectile launch consume the same frame pose. The visible barrel follows eye-forward; projectile direction converges from the physical muzzle to the crosshair target before authored spread. Jeep Gun supplies100armor-piercing damage,0.12second cadence,0.1second warmup and999ammo. The existing eight-flight bounded primary path excludes host/occupant and resolves world/mover/rubble/actor contacts. Its diagnostic label remains APC_PRIMARY for both vehicle classes.

PC verification: 460-frame gunner replay shows the forward view, vehicle chassis and role/ammo HUD; five accepted launches and five contacts leave994ammo. The600-frame replay also returns to driver and safely exits (one entry, two role switches, one exit). Focused actual-asset seat transfer tests cover blocked head clearance, held debounce, parked gating, independent gunner orientation under host rotation and retained ownership. Gun resource/aim and cockpit tests pass. NXDK build succeeds. Native run `artifacts/xemu/render-20260918-154013` passes460frames on stock64MiB, with matching PC/Xbox vehicle, seat and primary-fire state and3117freepages (12.18MiB). Native framebuffer inspected: forward gunner view, chassis and correct994ammo/role HUD. Harness closed XEMU and restored the disc. Native return-to-driver/exit has not been separately exercised; PC covers that path.

Remaining: campaign placement, NPC drivers/passengers, general campaign saves, live NPC combat, full sound verification, muzzle flash/tracer polish and broader destruction/ejection coverage. APC/Jeep DEV checkpoint integration is described below.

## Save and destruction integration work

The public160-byte Jeep record now preserves health, rigid motion, driver/gunner role, gun reserve/RNG, relative aiming and the independent gunner reference basis. Focused codec round trips and malformed-state rejection pass. The live DEV save/load transaction now consumes this record, as described below.

A focused test using actual Jeep resources, registration, possession and damage adapters passes for lethal damage in both seats. A controlled blocked world retains the living occupant until a clear standing exit exists; successful release clears links/control once and restores preboarding look. Dead-occupant cleanup also passes while the path remains blocked. This establishes adapter behavior, not native visual destruction or actual-level ejection coverage.

RFCP3 now dispatches to the actual Driller/APC/Jeep codecs (128/128/160bytes) and class-aware seated-player validation. Focused envelope checks cover parked/seated records for all three classes, retained5000HP/APC, independent Jeep reference, malformed profile rejection and legacy save compatibility. Existing Driller composed and seated checks pass.

Typed combat-vehicle capture and pure restore staging pass focused checks for actual health limits, ammo/RNG/role/aim, angular momentum conversion and rejection without partial output. Capture rejects active projectiles, pending firing/seat input, burns and stale ownership. Restore stages current authored inertia and saved motion without mutating the scene. Live integration now retains exact class identity, selects the saved Jeep seat before placement, validates restored world/seat clearance, restores independent aim, and publishes player/vehicle state together. General campaign save coverage remains open.

## Live checkpoint wiring

Live RFCP capture/load now dispatches by exact vehicle class, checks restored chassis and seat against staged terrain and detached pieces, then rebuilds semantic occupancy using fresh registry identity. Jeep gunner orientation is installed before attachment publication; relative aim and finite ammunition resume afterward. On-foot exit look remains the preboarding look. Pending weapon/seat actions cannot be silently discarded.

`tools/check_combat_vehicle_checkpoint.py --vehicle jeep --run` passes720-frame occupied capture,60-frame seated continuation and180-frame return-to-driver/exit on PC. Ammo994, health400, primary RNG, gunner role and independent aiming reference survive reload; resumed and on-foot framebuffers inspected. Stock64MiB native run `artifacts/xemu/render-20260918-155426` passes60-frame seated continuation, with identical1228-byte PC/Xbox checkpoints and11.97MiB free. Native gunner view/HUD inspected, disc restored and emulator closed. Native exit remains separately unverified.
